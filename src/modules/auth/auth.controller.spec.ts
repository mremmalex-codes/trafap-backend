import { describe, it } from "vitest";
import supertest from "supertest";

const request = supertest();

describe("describing all authentication endpoints", () => {
    it("register", async () => {});
});
